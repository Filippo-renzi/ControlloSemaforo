*AVVERTENZE*
Per far partire il programma avviare il file ".sln" con una qualsiasi versione di .VS
ricordatevi di sdcaricare tutto se no il programma non funzionera correttamente

Se si vuole implementare del codice si può fare tranquillamente, basta solo che poi lo condividiate

*WARNINGS*
To start the program run the ".sln" file with any version of .VS
remember to download everything otherwise the program will not work properly

If you want to implement the code you can do it easily, just then you need to share it
